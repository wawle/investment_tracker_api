export enum Currency {
  TRY = "try",
  EUR = "eur",
  USD = "usd",
}

export enum Market {
  DownJones = "down-jones",
  Crypto = "crypto",
  Nasdaq = "nasdaq",
  Bist100 = "bist100",
  SP500 = "sp500",
  Electronic = "electronic",
}

export enum AssetMarket {
  TRStock = "tr-stock",
  USAStock = "usa-stock",
  Exchange = "exchange",
  Fund = "fund",
  Commodity = "commodity",
  Crypto = "crypto",
}

export enum TransactionType {
  Sell,
  Buy,
}
